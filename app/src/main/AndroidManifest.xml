<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uofa.assignment1habittracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddHabitActivity"
            android:label="@string/title_activity_add_habit" />
        <activity
            android:name=".ViewAllHabits"
            android:label="@string/title_activity_view_all_habits" />
        <activity
            android:name=".InspectHabitView"
            android:label="@string/title_activity_inspect_habit_view"></activity>
    </application>

</manifest>